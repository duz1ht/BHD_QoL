# Dev environment for the local NovaWorld stack. Committed on purpose:
# these are local-only, non-secret values. Prod values come from the
# 1Password vault via the deploy toolbox (see DEPLOY.md), never from a
# committed file.

# The server advertises this host in its gate response. 127.0.0.1 is the generic
# fallback. NOTE: the host/join peer-to-peer config (ONNET_PUBLIC_HOST plus the
# ONNET_CLIENT_REFLECT_* vars: IP, GATE_PORT, NOVAWORLD_PORT) is MACHINE-SPECIFIC
# and set in deploy/compose/docker-compose.dev.yml's `environment:` block — the
# base compose's `environment:` overrides this env_file for ONNET_PUBLIC_HOST, so
# setting it here has no effect.
ONNET_PUBLIC_HOST=127.0.0.1

# Local admin basic-auth for the web /admin area (dev only).
ADMIN_BASIC_AUTH_USER=admin
ADMIN_BASIC_AUTH_PASSWORD=admin

# Enables the admin API on the C++ server (dev token; rotate in prod).
ADMIN_API_TOKEN=dev-admin-token
